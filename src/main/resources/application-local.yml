spring:
  config:
    activate:
      on-profile: local
  datasource:
    url: jdbc:mysql://localhost:3306/~~~~~?serverZoneId=Asia/Seoul&sslMode=DISABLED
    username: ~~~~~
    password: ~~~~~
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  batch:
    jdbc:
      platform: mysql
      initialize-schema: always
    job:
      enabled: false

logging:
  level:
    com.zaxxer.hikari: INFO
    com.zaxxer.hikari.HikariConfig: INFO
    com.zaxxer.hikari.pool.HikariPool: INFO
    org.springframework.batch: INFO
    org.springframework.jdbc: INFO
    org.springframework.boot.autoconfigure.jdbc: INFO
    com.example.springbatchkotlin: INFO

